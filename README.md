# Escala de Ministros — React + Supabase

## Variáveis (.env.local)
Crie `.env.local` com:
```
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
```

## Rodar
```bash
cd client
npm i
npm run dev
```
Abra http://localhost:5173